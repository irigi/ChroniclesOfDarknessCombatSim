// Splat-specific rules modules (Phase 3)
// Currently empty — splat logic lives in combat.rs for Phase 1.
// Each splat will get its own module as the rules are formalized.

pub mod vampire;
pub mod werewolf;
pub mod changeling;
